{
  "name": "bundler",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rm -rf ./.parcel-cache && parcel ./index.html --port=3000 --host=127.0.0.1 --open",
    "build": "rm -rf ./build && parcel build ./index.html --dist-dir=./build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.10.1",
    "buffer": "^6.0.3",
    "crypto-browserify": "^3.12.0",
    "events": "^3.3.0",
    "os-browserify": "^0.3.0",
    "parcel": "^2.10.1",
    "path-browserify": "^1.0.1",
    "process": "^0.11.10",
    "sass": "^1.69.5",
    "stream-browserify": "^3.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "moment": "^2.29.4"
  }
}
